# User Behavior Analytics Pipeline

A Python pipeline for processing event-level user data and producing daily product metrics.

The project includes:

- synthetic event data generation
- daily KPI calculation (DAU, purchasers, revenue, conversion rate)
- CSV output for downstream analysis

---

## Project Structure
